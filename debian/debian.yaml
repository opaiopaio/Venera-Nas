flutter_app: 
  command: veneranas
  arch: {{Arch}}
  parent: /usr/local/lib
  nonInteractive: true

control:
  Package: veneranas
  Version: {{Version}}
  Architecture: {{Architecture}}
  Priority: optional
  Depends: libwebkit2gtk-4.1-0, libgtk-3-0
  Maintainer: haukuen
  Description: VeneraNas
  
#options:
#  exec_out_dir: debian/packages
